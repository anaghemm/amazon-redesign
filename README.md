# Primary Care Management System

Database project for BTH.

## Files
- app.py – Flask application
- databas.sql – Database schema and sample data

## Run
1. Import database:
   mysql -u root -p < databas.sql

2. Start app:
   python app.py
